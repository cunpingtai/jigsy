import { SchedulerControl } from "@/components/shared/SchedulerControl";

export default function CronPage() {
  return (
    <html>
      <body>
        <SchedulerControl />
      </body>
    </html>
  );
}
